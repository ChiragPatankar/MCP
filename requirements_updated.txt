# Updated requirements.txt for Gemini-powered MCP Server
# Based on your existing requirements with Gemini replacing Cursor AI + Glama.ai

# Core FastAPI dependencies (keep your existing versions)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Google Gemini AI (NEW - replaces Cursor AI + Glama.ai)
google-generativeai==0.3.2

# Database dependencies (keep your existing ones)
psycopg2-binary==2.9.9
sqlalchemy==2.0.23

# HTTP clients (keep existing)
aiohttp==3.9.1
httpx==0.25.2
requests==2.31.0

# Environment and configuration
python-dotenv==1.0.0

# CORS and middleware
python-multipart==0.0.6

# Authentication & Security (if you're using these)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Add any other dependencies from your original requirements.txt
# Just remove these if you had them:
# - glama-ai-client (or whatever you used for Glama.ai)
# - cursor-ai-client (or whatever you used for Cursor AI) 